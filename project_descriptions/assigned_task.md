# Assigned Task to be completed

## Business Profile - Completed
- Storing & Fetching the business profile data from the database (The database is in Supabase and the necessary tables and columns have been created. We will need to write the necessary queries to fetch the data and display it on the map. We will also need to implement the functionality to update the business profile data if needed. The business profile data will include information such as the business name, location, industry, and any other relevant details that can help provide context for the scores displayed on the map. Check database_schema.sql for the details of the database schema and the relevant tables and columns for related data to be linked to the newly created business profile database.)

## Scoring + AI Chatbot Explanation - Completed
Score explanation (Only to be shown and usable on the "Map", "Portfolio" and "Compare" pages for now, as these are the pages where the scores are displayed and where users would most likely want to understand the reasoning behind the scores. We can consider expanding this functionality to other pages in the future if there is a need for it.)
- Send data to the chatbot api for explanation (Google Gemini) (API to be left empty for now, we will fill it in later when we have the API key) (Due to API limitations, we will need to be strategic about the data we send to the chatbot. We will need to prioritize the most relevant information that can help the chatbot provide a comprehensive explanation of the score, while also being mindful of the amount of data we are sending to avoid hitting any API limits. We may need to experiment with different approaches to find the right balance between providing enough information for a detailed explanation and staying within the API limits.)
- Send data to the chatbot api with a system prompt for conversation-style interaction (The system prompt should be detailed and should include instructions for the chatbot to explain the score in a way that is easy to understand for the user, and also to provide insights on how to improve the score if possible. The data sent to the chatbot should include the score and any relevant information that can help the chatbot provide a comprehensive explanation. The response from the chatbot should be displayed in a user-friendly format, such as a pop-up or a dedicated section on the map, where users can easily read and understand the explanation of the score. Will decide what are the necessary information to be sent to the chatbot: whether to just send the scores shown on the page or to also include other relevant data that can help the chatbot provide a more detailed explanation. This will depend on the complexity of the scoring system and the amount of information available on the page. We will need to experiment with different approaches to find the most effective way to communicate with the chatbot and provide valuable insights to the users.)
- We try not to send any data from the database to avoid complexity, but if there is a need (can try to send a screenshot of the page with the score or use a web scrape tool to send the score data to the chatbot).

